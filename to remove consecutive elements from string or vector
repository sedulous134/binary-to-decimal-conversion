vector<int> vec = {10, 20, 20, 20, 30, 30, 20, 20, 10};

	auto it = unique(vec.begin(), vec.end());

	vec.resize(distance(vec.begin(), it));

	for (it = vec.begin(); it != vec.end(); ++it)
		cout << ' ' << *it;

	//this removes consecutive elements from the container
